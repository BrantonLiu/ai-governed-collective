---
id: TAS-20260311-CMT8
author: "@branton"
status: reviewed
timeSpent: 10
category: code
createdAt: "2026-03-11T02:53:27.655Z"
aiScore: 82
sociallyNecessaryTime: 8
---

# 任务描述

build the mvp of agc core

# 交付物/证据链

- ./agc
- ./.agc

# AI Review Reasoning

## Score: 82 / 100 | Socially Necessary Labor Time: 8 hrs

### Evaluation Summary

This task delivered the **foundational MVP of the Agentic CLI (`agc`)**, which serves as the protocol execution layer for the AI-Governed Collective. The deliverables include:

1. **CLI Entry Point** (`agc/src/index.ts`) — Commander.js-based CLI routing with `task` and `score` subcommands.
2. **Task Commands** (`agc/src/commands/task.ts`) — Interactive task submission via `@clack/prompts`, task listing, and the BYOA review prompt generator.
3. **Score Commands** (`agc/src/commands/score.ts`) — Contribution scoreboard aggregating SNLT and raw AI scores from reviewed tasks.
4. **File Store** (`agc/src/core/file-store.ts`) — Markdown + YAML Frontmatter based append-only storage engine, with hash-free Git-native auditability.
5. **BYOA Prompt Generator** — The `agc task review` command generates structured prompts for any local Agent (Cursor, Antigravity, Claude Code, etc.) to execute arbitration, embodying the Bring-Your-Own-Agent philosophy.

### Strengths (+)

- **Architectural clarity**: Clean separation of concerns (commands / core / agent layers).
- **BYOA paradigm**: A genuinely novel design decision — instead of hardcoding an AI API, the CLI acts as a protocol generator for any Agent. This is philosophically aligned with the Protocol's decentralization principles.
- **Git-native transparency**: Using Markdown + Frontmatter means every contribution record is human-readable, version-controlled, and auditable without special tooling. This directly serves Protocol III.3 (Full Transparency).
- **Functional end-to-end flow**: submit → list → review → score is a working pipeline.

### Deductions (−)

- **−8: Limited error handling**: Edge cases (malformed YAML, missing directories, concurrent writes) are not handled. The `listTasks` function silently swallows parse errors.
- **−5: No tests**: Zero automated tests shipped. For a system whose core promise is "objectivity and auditability", the absence of tests is a notable gap.
- **−5: Sparse documentation**: The task description itself ("build the mvp of agc core") is minimal. Evidence links point to directories rather than specific commits or PRs, making precise attribution harder.

### Socially Necessary Labor Time Estimate

An experienced TypeScript developer familiar with Commander.js and @clack/prompts could reproduce this scope of work (5 source files, ~250 lines of functional code, dependency setup, architectural decisions) in approximately **8 hours**. The contributor reported 10 hours, which is within reasonable range for a first implementation involving design exploration (demo-clack.ts, demo-ink.tsx indicate prototyping work).

---

_Reviewed by: AI Arbiter (Antigravity) | Date: 2026-03-11_
